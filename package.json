{
  "name": "taskmanager",
  "private":true,
  "version": "1.0.0",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend":  "cd springboot && ./mvnw spring-boot:run",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
